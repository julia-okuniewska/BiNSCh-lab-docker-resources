FROM ubuntu:latest

RUN apt-get update && apt-get install -y \
    python3.8 \
    python3-pip

COPY /target/script.py /usr/src/docker_volume/script.py

WORKDIR /usr/src/docker_volume

CMD ["/bin/sh"]

